			; Map 18 init
			; THE EMPEROR!! (Or Wizard?)
            
Map18Init:  lda #$01
            sta MapVar1
            sta MapVar2
            lda #$00
            sta SIZEP2
            sta SIZEP3
            sta L39CF
            sta L39D0
            sta L39D1
            lda #$1B
            sta L39D2
            lda #$06
            sta L39D7
            lda #$07
            sta L39D8
            rts
